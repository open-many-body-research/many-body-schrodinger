> **Archived research note (v1.0 foundation).** Written during AI-assisted exploratory work before publication. Labels such as "PROVEN", "independently reviewed" or "sealed" in this file are the original authors' and are **not** evidence tiers. The authoritative status of every claim is in [`claims/registry.yaml`](../../../../claims/registry.yaml) and [`STATUS.md`](../../../../STATUS.md). File paths refer to the private workspace layout.

# Both nuclear-chart finite weak H12 initializations

For each selected i in Fin 2 and real Z,E, the formal theorem now establishes the same physical order-twelve initialization on the actual NuclearKSSpace i. It uses its own compact annulus K_i and volume(K_i), so no equivalence between different spectator measures is needed. There are finite KT>=1, KY>=1, QY>=0 selected before the unit spectator center t0, scalar graph input, continuous AE representative g, Lipschitz constant L, radius R and epsilon.

If g is L-Lipschitz on ball(0,R), 0<epsilon<=min(1,R/4), and the scalar Coulomb graph relates f to E f, then ((g(epsilon x)-g(0))/epsilon) composed with nuclearKSLift i has genuine coordinate weak jets of total order <=12 on the physical inverse image of the centered box with widths 1/128. Every squared integral is at most (L^2+|g(0)|^2) times ksH12MixedBudgetAt i 4 96 14016 KT KY QY. The recurrence composes 12 spectator steps and 5 spatial steps, using the unchanged physical 34-gap schedule from width1/64.

The nuclear physical c=4 equation is derived from the actual graph; selected-coordinate reindexing affects the presentation of boxes and tests only. A second exact theorem retains this coordinate predicate and constructs the physical multiindex family: all 50388 indices of total order <=12, zero member equal to the pullback, each budget as above, the aggregate bound 50388 times that budget, and the full integral weak derivative identities against compact real smooth tests.

Together with the separately sealed pair endpoint, this discharges the finite scalar chart initialization conditional on the graph/representative/Lipschitz inputs. It does not derive those physical eigenfunction inputs, provide an all-order or factorial bound, identify an independent conventional Sobolev norm, evaluate the noncomputably chosen coefficient bounds, or prove full Theorem T.

All new exact Lean 4.34.0-rc2 statements compiled and passed a joint v6 expanded-statement and axiom audit. Only propext, Classical.choice and Quot.sound occur. Existing pinned dependencies were reused; no isolated source rebuild was attempted for this unit.
