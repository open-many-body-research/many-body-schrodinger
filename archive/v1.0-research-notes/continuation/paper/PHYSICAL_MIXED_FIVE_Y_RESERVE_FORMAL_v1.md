> **Archived research note (v1.0 foundation).** Written during AI-assisted exploratory work before publication. Labels such as "PROVEN", "independently reviewed" or "sealed" in this file are the original authors' and are **not** evidence tiers. The authoritative status of every claim is in [`claims/registry.yaml`](../../../../claims/registry.yaml) and [`STATUS.md`](../../../../STATUS.md). File paths refer to the private workspace layout.

# Physical mixed source data and five Y stages

For the actual physical nuclear and pair KS coefficients, write b_epsilon for the bare coefficient at energy epsilon E and P_epsilon=epsilon b_epsilon. The exact source is b_epsilon smul (-a0). Its mixed words are S_(a,t)=(D_Y^a D_T^t b_epsilon) smul (-a0), with the existing outer-first word order. Local integration by parts and the true local product rule prove their local L² membership, all Y-prefix weak derivative links and the required pure-T weak links. On a measurable region S where the mixed coefficient word is bounded by K0, its squared source integral is at most K0² norm(a0)² volume(S).

The exact mixed-word comparison evaluates the iterated Frechet derivative on the ordered coordinate tuple. Every actual Y or T basis direction has norm one in the ordinary product norm, so the comparison has factor one. The previously proved finite physical Frechet bounds therefore supply a single K0 for all mixed source/coefficient words of total order≤10, chosen before epsilon, amplitude and region.

The physical five-stage endpoint fixes arbitrary compact K inside the relevant coefficient patch. It chooses K0≥1 and Q0≥0 before geometry, epsilon, amplitude and raw solution. The quadratic weight bound Q0 is derived automatically: compactness bounds norm(Y) by R≥0, and every actual Y-word derivative of norm(Y)² is bounded by R²+2R+2. First and second derivatives are explicit and every order≥3 vanishes.

For actual five-stage Y cutoff geometry, open target regions and Omega0 subset K, epsilon in [0,1], a genuine initial SpectatorFiniteState(Omega0,f,12,Winit) and the original weak equation (P_c+P_epsilon)f=b_epsilon smul (-a0), the endpoint constructs MixedTriangularState(Omega5,f,12,12,R5). Nuclear c=4 and pair c=1 are retained. R5 is the explicit existing Y recurrence with source budget K0² norm(a0)² volume(Omega0). No mixed coefficient bound, forcing derivative, quadratic derivative or higher-Y solution derivative remains as a separate input. Epsilon=0 is an admissible abstract PDE case; physical normalized-difference composition uses positive epsilon separately.

Evidence: three new final modules, seven declarations, strict expanded-statement and axiom audit PASS with only standard foundational axioms. Actual graph/eigenfunction and concrete scheduled-box composition remain separate. The output is the actual mixed derivative reserve, not yet an aggregate Sobolev norm, a factorial estimate, C-infinity conclusion, executable algorithm or full Theorem T.
