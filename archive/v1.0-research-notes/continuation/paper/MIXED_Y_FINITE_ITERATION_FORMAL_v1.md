> **Archived research note (v1.0 foundation).** Written during AI-assisted exploratory work before publication. Labels such as "PROVEN", "independently reviewed" or "sealed" in this file are the original authors' and are **not** evidence tiers. The authoritative status of every claim is in [`claims/registry.yaml`](../../../../claims/registry.yaml) and [`STATUS.md`](../../../../STATUS.md). File paths refer to the private workspace layout.

# Five-stage recovery of the full mixed order-twelve reserve

Let P_c=-Delta_Y-c|Y|²Delta_T and let B be real smooth on the initial open region. Starting from an actual MixedTriangularState(Omega0,f,r,m,W0) with r≥1, n supplied cutoff stages for the Y-Laplacian produce MixedTriangularState(Omega_n,f,r+2n,m,R_n). The only equation input is the original genuine compact-test equation (P_c+B)f=S_empty,empty. Each stage derives its differentiated equations and new weak derivative witnesses using the verified potential-step theorem.

The source is supplied as actual mixed weak words through total order m−2, with common region L² budget H0 and the requisite weak links below that order. Actual coefficient words D_Y^a D_T^b B and quadratic words D_Y^a|Y|² through the same total reserve are bounded by K0 and Q0. These data are restricted to each smaller region; the same original raw f and PDE remain in force. The original principal coefficient c is retained. The cutoff geometry uses c=0 because each stage applies the Y-Laplacian recovery theorem.

With d=card(kappa), define U(W)=3H0+3(2^m)²K0²W+3c²(2^m)²d²Q0²W. For the kth cutoff data M_k,A_k,L_k,D_k,Q_k, the explicit budget recurrence is R0=W0 and

R_(k+1)=max(R_k, (3/2)[(4A_k²+16L_k(D_k/2+Q_k))R_k+(2M_k²+8L_kD_k)U(R_k)]).

The exact five-stage corollary starts from the previously constructed SpectatorFiniteState(Omega0,f,12,W0). Its existing Y/YY witnesses supply r=2 without increasing the budget. Five actual stages yield MixedTriangularState(Omega5,f,12,12,R5), using source/coefficient words only through total order ten. Thus all canonical mixed words with |a|+|b|≤12 are represented by actual local L² functions with genuine Y/T weak links. The conclusion is per word, not an aggregate over all multiindices and not yet an identification with a separately defined H12 norm.

The recurrence is monotone in source and initial budgets. For a nonnegative common factor r, simultaneous scaling of H0 and W0 scales R_n by exactly r. Therefore H0≤r Hbase and W0≤r Wbase imply R_n≤r R_n(Hbase,Wbase), including r=0 without division. The nonnegative-scale hypothesis is explicit because max does not commute with negative scaling.

Evidence: four exact Lean modules, twelve declarations, strict audit with standard axioms only. Actual scheduled physical geometry, eigenfunction amplitude/PDE application and the final finite multiindex norm assembly are separate ongoing compositions. No factorial estimate, analytic regularity conclusion, computational procedure or full Theorem T is asserted here.
